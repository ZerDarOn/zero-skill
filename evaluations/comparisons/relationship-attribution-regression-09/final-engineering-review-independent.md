# 第十六轮最终独立工程复核

## Findings

无阻塞性或非阻塞性工程发现。

## 结论

**可提交。**

本结论只覆盖当前工作区中的第十六轮 `relationship-review` 0.1.4 交付，不把本轮结果扩大为正式发布、`verified` 成熟度、隐式路由通过、真实关系场景验收或一般嵌套来源链能力已解决。

## 核对证据

- 从机器报告的 45 个内嵌匿名评审项逐候选重算，得到 117 个输出、351 个布尔标准；两个阶段各臂的硬标准、完整输出和偏好数均与发布汇总一致。
- 第二阶段三个新增用例重算为：baseline `21/27`、`3/9` 完整、2 次偏好；0.1.3 `17/27`、`3/9` 完整、0 次偏好；0.1.4 候选 `24/27`、`6/9` 完整、7 次偏好。
- 六个复用用例重算为：baseline `51/54`、`15/18` 完整；0.1.3 `52/54`、`16/18` 完整；0.1.4 候选 `54/54`、`18/18` 完整。逐题未发现候选相对 0.1.3 退化。
- 三个新增用例的前两项来源归因标准重算为 `12/18`、`8/18`、`15/18`；加上门禁卡用例第一项后为 `12/21`、`9/21`、`18/21`，与文档和机器报告一致。
- `nested-handoff-report-chain` 的候选结果重算为 `7/9`、`1/3` 完整；前两次来源链压平的残留已在 README、轮次报告、机器报告和决策复核中明确保留，没有宣称嵌套转述已普遍解决。
- 当前 catalog 登记为 `relationship-review` 0.1.4、`experimental`、`evidence: null`；关系技能活动用例 14 项，全仓活动用例 102 项，README 与评测说明数字一致。
- 当前 0.1.4 包指纹与机器报告决策指纹一致；0.1.3 历史报告已改为指向 `evaluations/comparisons/relationship-attribution-regression-09/packages/relationship-review-0.1.3`，不会被当前技能目录升版破坏。
- 第十五轮历史测试移除了会随当前版本和活动用例总数自然失效的断言，改为校验冻结的 0.1.3 快照；第十六轮测试重新承担当前 0.1.4、14/102 用例数、包指纹及新增用例存在性的门禁，因此未削弱必要约束。
- 文档明确说明本轮使用合成材料、显式加载和模型盲评，评审者看过协议且第二阶段看过修订；没有把结果表述为独立人工验证或隐式路由验证。

## 验证记录

- `git diff --check`：通过。
- `python scripts/validate_collection.py`：通过。
- `python -m unittest discover -s tests -v`：66 项，65 项通过，1 项因 Windows 无符号链接权限跳过；无失败。

## 非阻塞残余风险

- 第十六轮回归测试会从内嵌输出重算各臂总体统计，但新增/复用/来源归因子统计目前主要以固定期望值断言；本次已人工独立逐项重算并确认一致。未来若继续修改报告生成流程，可把这些分组统计也改为程序化派生，以降低保持总体不变但组内分布漂移时的遗漏风险。
- `prepared_hashes` 与 `artifact_hashes` 在提交态测试中只校验摘要格式；冻结来源文件、内嵌输出摘要和技能包指纹已有实质校验，但被忽略的原始运行目录仍不属于可由干净检出独立复现的提交证据。
