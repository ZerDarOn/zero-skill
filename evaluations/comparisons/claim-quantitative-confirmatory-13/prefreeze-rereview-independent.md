# 冻结前独立复审（二审）

## 结论

**可冻结。** 本次修订已解决上一轮指出的标准归属与等价百分比歧义；当前未发现阻塞项或新的非阻塞歧义。

## 复审发现

- `corrected-and-missing-denominators` 的核心遗漏现仅按标准 1 计数。标准 1 只要求保留北区 `12/60 → 12/80`、当前 `15%`，以及南区已知分子 `9`、分母缺失这组两区定量状态；来源关系已独立放入标准 2，总体结论已独立放入标准 3。三项边界清楚，不会把来源关系或结论遗漏误计为核心定量遗漏。
- `strict-table-two-batches` 的 README 已明确标准 2 接受完整的等价百分比表达，与案例硬标准一致，不再要求只能复述原始分数。
- 三个案例的算术与判定边界一致：`5/20 = 125/500 = 25%`，但样本量不同；北区由 `12/60 = 20%` 更正为 `12/80 = 15%`，南区因分母缺失不能确认比例；A 批次 `10% → 5%`、减半，B 批次 `10% → 7.5%`、下降 `2.5` 个百分点（相对下降 `25%`），不能写成减半。
- 严格表格案例的格式要求来自用户题面，且明确排除无关响应时长与满意度数据，未发现不公平的隐藏要求。
- 结构检查通过：3 个唯一案例 × 2 个实验臂 × 每例 5 次重复 = 30 个计划输出；每例均有 3 条硬标准。
- 完整硬标准文本未出现在公共提示或案例题面中，未发现硬标准泄漏。
- 晋级门槛明确且排除了上一轮揭示后的 `4:2` 结果：只有新确认轮中 ours 在至少两个案例各出现 `≥2/5` 核心遗漏，且 baseline 在对应每个案例均为 `≤1/5`，才进入候选设计阶段；即便触发，也不直接修改、升版或发布技能。

## 复审范围

本次仅审阅修订后的 `README.md`、`promptfoo.json` 与 `cases.json`，并进行只读结构与完整硬标准泄漏检查。未运行模型，未修改协议文件，也未改动原始 `prefreeze-review-independent.md`。
