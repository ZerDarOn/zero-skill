# 第五轮质量复核：产品背景的开放范围与来源冲突

日期：2026-09-10。本轮对 `product-context-brief` 补充回归与完整证据，**包正文、指纹和 0.1.0 版本均未改变**。已有 16 个技能继续保持 `experimental`。

## 本轮检查

新增三个合成案例，在运行前冻结：

- **限量开放**：CSV 已对两个套餐开放，但 SSO 仅在企业版 10 个受邀租户中试用；不能写成所有企业客户可用，也不能把销售下周演示的意愿写成工程承诺。
- **来源冲突**：较新的销售笔记声称全面上线，与此前正式发布记录矛盾；不能只按更新时间覆盖，也不能将保守沟通口径说成实际状态已经核实。
- **仅交当前稿**：用户已另存历史，要求只返回更正后的三行背景；需要更新样本数及采购角色，不追加历史、旧值或版本号。

初次三个案例全部通过，没有观察到需要修改指令的实质性错误，因此保留包并独立复跑全部七个活动案例。新增三个案例在两次同包运行中均通过，也在无技能组通过；这是覆盖补充，不是新增能力或版本改进。

## 模型诊断

Codex CLI 0.153.4，`gpt-5.6-sol`、`medium`；合成材料、完整包显式加载、忽略用户配置、临时只读会话。提示禁止工具，实际调用为 0。所有评分由实施代理阅读回答完成，没有独立人工评审。

| 组别 | 冻结全约束通过 | 说明 |
| --- | --- | --- |
| 无技能 | 6/7 | 全部七例 |
| 同包初次抽测 | 3/3 | 仅新增三例 |
| 同包完整复跑 | 7/7 | 全部七例，三个新增案例各获得第二次运行 |

共 **17 次有效生成**，无重试、无排除、无评分标准修改。记录中的 `old` 和 `current` 是运行器组名，两组装载的版本、字节和指纹完全相同，不代表新旧两个版本。

无技能唯一严格未通过项是产品背景遗漏 `Q1` 引语来源编号；客户原话、内部想法、试点范围及路线图区分仍正确。按核心内容理解，两组完整运行均为 7/7，不能把来源编号差异宣传为产品理解能力提升。

## 必须保留的限制

1. 原“有界更新”标准检查 Product 与 Voice 正文逐字保留，完整复跑确实满足。但回答在标题后加了 Markdown 换行空格，所以通过不证明整个章节的所有字节都不变。要求严格无损编辑时，未来应单独验证完整受保护片段。
2. 角色摘要里的宽泛工作支持需求，是对给定活动的概括，不是已验证的客户需求或新增产品能力。具体需求仍需材料确认，不据此推导采购权限。
3. R1 是最后提供的正式记录，不代表实际状态此后不可能改变。两组都没有把更新较晚的销售笔记作为已核实上线依据；保守口径仍应保留待确认状态。
4. 两次同包样本不足以证明稳定性；这些是实施者设计的已知案例，不是盲测，也没有自动发现、工具执行、跨宿主或原版上游对照。

完整七例的输出 token 为无技能 860、技能 1,009。没有观察到输出成本优势；一次样本不能据此确定稳定的成本或延迟差异。输入、缓存和本地各次时长保存在证据中。

## 交付

[完整诊断证据](../evaluations/reports/quality-polish-round-05-diagnostic.json)保存全部 17 次精确提示、输出、事件、stderr 和哈希，逐条评分、冻结用例、相同包的两份快照、运行器及导出清单。每次提示包含完整用户材料，不含评分标准。

活动用例从 91 增至 94。沿用前轮覆盖清单并补入这三例，按当前版本与用例 ID 为 90/94 已有运行记录；剩余四例仍为自动发现用例，不表示其他 90 例全部通过。

已有 `dist/product-context-brief-0.1.0.zip` 与当前包成员、字节及清单指纹一致，直接复核原归档，没有重新生成、覆盖或全局安装。技能仍为 `experimental`、`evidence: null`。

结构与技能入口检查通过；单元测试 26 项通过、1 项因 Windows 符号链接权限跳过。文档链接与空白检查通过。本轮及此前第三、四轮修改尚未提交或推送。
