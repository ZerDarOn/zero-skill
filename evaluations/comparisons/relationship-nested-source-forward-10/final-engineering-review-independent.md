# 第十七轮最终独立工程复核

## Findings

无阻塞性或非阻塞性工程发现。

## 结论

**可提交。**

本结论只覆盖当前工作区中的第十七轮多跳来源前向诊断交付，不把本轮结果扩大为 Skill 升版、`verified` 成熟度、隐式路由通过、独立人工验收或一般来源链能力已经完全解决。

## 核对证据

- 从机器报告内嵌的 12 个匿名评审 item 逐候选重算，得到 24 个输出、72 个非空布尔标准和 4 个平局；每项 review notes 均非空，平局的 `preferred_candidate` 与 `preferred_arm` 均为 JSON `null`。
- 总体重算为 baseline `32/36`、`8/12` 完整、2 次偏好，0.1.4 `36/36`、`12/12` 完整、6 次偏好，与报告、轮次文档和 README 一致。
- 逐题重算为：更正后的多跳值班传话 `6/9、0/3 → 9/9、3/3`；有限日历与多跳更新 `8/9、2/3 → 9/9、3/3`；多跳发布窗口群消息两臂均 `9/9、3/3`；直接负责人最终更新两臂均 `9/9、3/3`。偏好分布分别为 `0/3`、`1/2`、`1/1`、`0/0`。
- 三个多跳题分组重算为 baseline `23/27`、`5/9` 完整、2 次偏好，0.1.4 `27/27`、`9/9` 完整、6 次偏好；直接权威控制两臂均 `9/9`、`3/3` 完整且无偏好。
- 当前 Skill 目录没有待提交差异；当前包与冻结的 0.1.4 候选快照指纹均为 `1f9ebb6ba4d394da7c638eb2f76f11407633c75ec6c92bb4c3d900af7f77de87`，版本保持 0.1.4，状态保持 `experimental`，`evidence` 仍为 `null`。
- `relationship-review` 活动用例现为 18 项，全仓活动用例为 106 项；新增四个回归用例均存在，原 `regression-nested-handoff-report-chain` 仍保留。
- 第十六轮历史测试只移除了会被后续活动用例增长自然破坏的 14/102 当前态断言，仍保留旧报告、旧包和旧残留门禁；第十七轮测试接管并校验 18/106、四个新增用例、旧残留存在、当前包指纹和本轮报告统计，因此没有削弱必要约束。
- tie→null 修正被如实记录：轮次文档与机器报告均说明第一次格式校验在揭盲前拒绝四个字符串 `tie`，原评审者只把这四个偏好字段改为 `null`，其他标准判断、说明和偏好不变；报告内嵌结果确有 4 个 null 平局，非平局偏好只映射到两实验臂。
- README、评测说明、轮次文档、机器报告和独立决策均明确保留第十六轮钥匙交接题 2/3 来源压平失败，并把本轮结论限定为三个新群消息表面通过；没有宣称一般来源链已经解决。
- 限制披露一致：全部材料为合成任务、只测显式项目 Skill 调用、评审者不是独立人工关系专家且此前接触过协议和修订、直接权威控制只有一题，也没有跨模型或跨语言验证。

## 验证记录

- `git diff --check`：通过。
- `python scripts/validate_collection.py`：通过。
- `python -m unittest discover -s tests -v`：67 项，66 项通过，1 项因 Windows 无符号链接权限跳过；无失败。

## 非阻塞残余风险

- 第十七轮测试从内嵌输出独立重算总体和多跳/控制分组，并与固定预期比较；报告 `analysis` 中的展示字段没有逐字段与重算对象绑定。本次人工复核已确认展示值完全一致，未来若生成流程继续演进，可直接断言重算对象与 `analysis` 相等以进一步防止展示层漂移。
- `prepared_hashes` 与 `artifact_hashes` 在提交态测试中仍以摘要格式检查为主；冻结来源、内嵌输出、输出摘要和技能包指纹已有实质校验，但被忽略的原始运行目录不属于干净检出可独立复现的提交证据。
