# React 性能审查 0.1.2 定向回归

本比较在第一次原生盲测揭盲后建立，不替换原始冻结结果。`promptfoo.json` 的正式运行冻结了无 Skill、原字节0.1.1和当时的0.1.2，两题各重复三次；以后当前包继续升版时，历史版本与指纹以冻结记录和本地快照为准。

- V01 把“可重复且不真实等待”写入用户要求，检验计时行为能否用可控时钟或等价调度验证。
- V02 把“键盘焦点必须跟随选中项”写入用户要求，检验虚拟化是否保留挂载后的焦点交接、稳定身份和完整集合语义。

0.1.1 快照包 SHA-256 为 `8ce9d88b07c40e62405b978ed1c21f272f27c14b8c6d311531ac7b23bf5369d0`；当前 0.1.2 包在准备运行时从 `skills/engineering/react-performance-review` 冻结，准备前指纹为 `aa85658bfa3905fed60127013b7a21150aeeef175539e09097d06bd798ee3496`。

这仍是静态审查回答的模型辅助盲评，不是运行 React 应用、浏览器、Profiler 或无障碍技术的验收。显式调用不证明隐式发现。

另用 `natural-prompt.json` 从第一次盲测原件选择 R02/R06，保持原始提示和冻结标准，比较自然任务中新增规则是否补足可控时钟与虚拟焦点细节。这个复跑不会覆盖第一次结果；R06 的焦点标准比可见题面更窄，必须单独报告严格分与核心任务解释。

最后用 `focus-prompt.json` 只选原始 R06，比较无 Skill、0.1.1、0.1.2 与当前 0.1.3，各重复三次。0.1.2 快照包 SHA-256 为 `aa85658bfa3905fed60127013b7a21150aeeef175539e09097d06bd798ee3496`；准备前的 0.1.3 指纹为 `66411b82b4075b8ad839cb75fce155ead7beff2170816591a6dab642116e05c6`。目标只检验方案是否说明挂载后焦点交接、验证是否检查实际焦点或有效活动项关系。
